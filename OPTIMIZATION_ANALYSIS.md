# Анализ алгоритма оптимизации ресурсов

## 📊 Текущий алгоритм: `optimize_with_task_shifting()`

### ✅ Сильные стороны:
1. **Простота и понятность** - легко понять логику работы
2. **Быстрота выполнения** - для небольших проектов работает быстро
3. **Учет временных ограничений** - проверяет границы сдвига (max_shift_days)
4. **Локальная оптимизация** - хорошо решает локальные проблемы перегрузки

### ❌ Слабые стороны и ограничения:

#### 1. **Жадный подход (Greedy Algorithm)**
- Оптимизирует только одну задачу за раз для каждой перегруженной недели
- Не учитывает комбинированные эффекты сдвига нескольких задач
- Может пропустить глобально оптимальные решения

#### 2. **Не учитывает зависимости задач**
- Не проверяет зависимости между задачами (predecessors/successors в MS Project)
- Может предложить сдвиг задачи, которая блокирует другие задачи
- Не учитывает критический путь проекта

#### 3. **Локальная оптимизация вместо глобальной**
- Оптимизирует каждую неделю независимо
- Не рассматривает эффект на весь проект
- Может создать новые перегрузки в других местах

#### 4. **Неэффективный поиск**
- Перебирает все возможные сдвиги от 1 до max_shift_days (O(n²) сложность)
- Для задачи с max_shift=30 дней проверяет 30 вариантов
- Для больших проектов становится медленным

#### 5. **Не учитывает характеристики задач**
- Игнорирует приоритеты задач
- Не учитывает критичность задач
- Не учитывает навыки ресурсов и соответствие задачам

#### 6. **Упрощенная модель**
- Строка 692: `hours_removed_from_source = task_hours` - упрощение
- Не учитывает частичное перекрытие задач с неделями при расчете улучшения
- Предполагает, что вся задача уходит из недели

## 🚀 Предложения по улучшению

### 1. **Глобальная оптимизация (Multi-objective Optimization)**

**Проблема:** Текущий алгоритм оптимизирует локально.

**Решение:** Использовать алгоритмы глобальной оптимизации:
- **Genetic Algorithm (GA)** - эволюционный поиск оптимальных решений
- **Simulated Annealing** - для больших пространств поиска
- **Particle Swarm Optimization (PSO)** - для многоцелевой оптимизации

```python
def optimize_with_genetic_algorithm(parser, settings, selected_resources=None):
    """
    Глобальная оптимизация с использованием генетического алгоритма
    
    Хромосома: список сдвигов для всех задач
    Фитнес-функция: 
      - минимизация перегрузки
      - максимизация равномерности распределения
      - минимизация общего времени проекта
    """
```

**Преимущества:**
- Находит глобально оптимальные решения
- Учитывает взаимосвязи между всеми задачами
- Может оптимизировать несколько целей одновременно

### 2. **Учет зависимостей задач**

**Проблема:** Игнорируются зависимости между задачами.

**Решение:**
```python
def check_task_dependencies(task_id, new_start, parser):
    """
    Проверяет, что сдвиг задачи не нарушает зависимости
    - Проверяет всех предшественников (predecessors)
    - Проверяет всех последователей (successors)
    - Учитывает типы зависимостей (FS, SS, FF, SF)
    """
```

**Преимущества:**
- Предложения будут реалистичными
- Не нарушит структуру проекта
- Учтет критический путь

### 3. **Машинное обучение для предсказания**

**Проблема:** Алгоритм не учится на предыдущих решениях.

**Решение:** Использовать нейросети для:
- **Предсказания оптимального сдвига** - обученная модель быстро предсказывает лучший сдвиг
- **Классификации задач** - какие задачи безопасно сдвигать
- **Оценки рисков** - вероятность того, что сдвиг создаст проблемы

#### Архитектура нейросети:

```python
import tensorflow as tf
from tensorflow import keras

def create_optimization_model():
    """
    Нейросеть для предсказания оптимального сдвига задачи
    
    Входные данные:
    - Характеристики задачи (длительность, приоритет, тип)
    - Загрузка исходной недели (процент, часы)
    - Загрузка целевой недели
    - Характеристики ресурса (навыки, текущая загрузка)
    
    Выходные данные:
    - Рекомендуемый сдвиг в днях
    - Ожидаемое улучшение (процент)
    - Уровень уверенности
    """
    model = keras.Sequential([
        keras.layers.Dense(128, activation='relu', input_shape=(20,)),
        keras.layers.Dropout(0.3),
        keras.layers.Dense(64, activation='relu'),
        keras.layers.Dropout(0.2),
        keras.layers.Dense(32, activation='relu'),
        keras.layers.Dense(1)  # Предсказание сдвига
    ])
    
    model.compile(
        optimizer='adam',
        loss='mse',
        metrics=['mae']
    )
    
    return model
```

**Преимущества:**
- Быстрая работа после обучения
- Учится на исторических данных
- Может находить неочевидные паттерны

### 4. **Рекомендательная система (Collaborative Filtering)**

**Проблема:** Не учитывается опыт предыдущих проектов.

**Решение:**
```python
def recommend_similar_shifts(current_task, historical_data):
    """
    Рекомендательная система на основе похожих задач
    
    Использует:
    - Размер задачи (часы)
    - Тип задачи
    - Загрузка ресурса
    - Похожие проекты из истории
    """
```

### 5. **Улучшенная модель оценки**

**Проблема:** Упрощенный расчет улучшения (строка 692).

**Решение:**
```python
def calculate_realistic_improvement(task, source_week, target_week, shift_days):
    """
    Точный расчет улучшения с учетом:
    - Частичного перекрытия задачи с неделями
    - Взаимодействия с другими задачами
    - Динамики загрузки во времени
    """
```

### 6. **Градиентный спуск для оптимизации**

**Проблема:** Полный перебор всех вариантов неэффективен.

**Решение:**
```python
def optimize_with_gradient_descent(parser, settings):
    """
    Использует градиентный спуск для поиска оптимальных сдвигов
    
    Функция потерь:
    - Сумма квадратов перегрузки по всем неделям
    - Штраф за слишком большие сдвиги
    - Штраф за нарушение зависимостей
    """
```

## 🧠 Практическое применение нейросетей

### Сценарий 1: Классификация задач для сдвига

```python
def should_shift_task(task_features):
    """
    Бинарная классификация: можно ли сдвинуть задачу?
    
    Использует:
    - Random Forest или Gradient Boosting
    - Входные признаки: тип задачи, размер, приоритет, загрузка ресурса
    """
    from sklearn.ensemble import RandomForestClassifier
    
    # Загрузить обученную модель
    model = load_model('task_shift_classifier.pkl')
    
    # Предсказать вероятность
    probability = model.predict_proba(task_features)[0][1]
    
    return probability > 0.7  # Порог уверенности
```

### Сценарий 2: Регрессия для предсказания сдвига

```python
def predict_optimal_shift(task_features, week_features):
    """
    Предсказывает оптимальный сдвиг в днях
    
    Использует нейросеть или Gradient Boosting Regressor
    """
    model = load_model('shift_predictor.h5')
    predicted_shift = model.predict([task_features, week_features])
    
    return int(round(predicted_shift[0]))
```

### Сценарий 3: Reinforcement Learning для адаптивной оптимизации

```python
def optimize_with_rl(parser, settings):
    """
    Использует Reinforcement Learning для обучения стратегии оптимизации
    
    Agent: Алгоритм оптимизации
    Environment: Проект с задачами и ресурсами
    Actions: Сдвиги задач
    Reward: 
      - Положительный за снижение перегрузки
      - Отрицательный за создание новых проблем
    """
    from stable_baselines3 import PPO
    
    # Загрузить обученную модель
    model = PPO.load("optimization_agent")
    
    # Получить текущее состояние проекта
    state = get_project_state(parser)
    
    # Предсказать оптимальное действие
    action, _ = model.predict(state)
    
    return action
```

## 📈 Рекомендуемый план улучшений

### Фаза 1: Улучшение текущего алгоритма (без ML)
1. ✅ Добавить проверку зависимостей задач
2. ✅ Улучшить расчет улучшения (точный учет перекрытий)
3. ✅ Добавить оптимизацию нескольких задач одновременно
4. ✅ Учитывать приоритеты и критичность задач

### Фаза 2: Глобальная оптимизация
1. ✅ Реализовать Genetic Algorithm
2. ✅ Добавить многоцелевую оптимизацию
3. ✅ Улучшить оценку качества решения

### Фаза 3: Машинное обучение
1. ✅ Собрать датасет из исторических проектов
2. ✅ Обучить модель классификации задач
3. ✅ Обучить модель предсказания сдвигов
4. ✅ Интегрировать в текущий алгоритм как фильтр/ускоритель

### Фаза 4: Продвинутое ML
1. ✅ Reinforcement Learning для адаптации
2. ✅ Рекомендательная система
3. ✅ Анализ временных рядов для предсказания загрузки

## 💡 Быстрые улучшения (можно реализовать сейчас)

1. **Бинарный поиск вместо линейного** - уменьшить количество проверок
2. **Кэширование расчетов** - не пересчитывать загрузку для каждого сдвига
3. **Приоритизация задач** - начинать с самых больших задач
4. **Учет критичности** - не сдвигать задачи на критическом пути без проверки

## 🔧 Технический стек для ML

- **TensorFlow/Keras** - для нейросетей
- **scikit-learn** - для классических ML алгоритмов
- **Optuna** - для оптимизации гиперпараметров
- **stable-baselines3** - для Reinforcement Learning
- **DEAP** - для генетических алгоритмов

## 📝 Выводы

Текущий алгоритм хорош для начала, но имеет существенные ограничения:
- Жадный подход не находит глобальных оптимумов
- Не учитывает зависимости между задачами
- Неэффективен для больших проектов

**Рекомендация:** 
Начать с улучшения текущего алгоритма (Фаза 1), затем добавить глобальную оптимизацию (Фаза 2), и только потом внедрять ML (Фаза 3-4), так как для ML нужны данные и время на обучение.

